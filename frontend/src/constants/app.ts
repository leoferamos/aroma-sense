/**
 * Application-wide constants
 */

/** Default placeholder image for products */
export const PLACEHOLDER_IMAGE = '/placeholder-product.svg';

/** Logo image path */
export const LOGO_PATH = '/logo.png';

/** Locale for formatting (currency, dates, numbers) */
export const APP_LOCALE = 'pt-BR';

/** Currency code */
export const CURRENCY_CODE = 'BRL';

/** Stock warning threshold (show "Only X left" below this) */
export const LOW_STOCK_THRESHOLD = 10;

/** Maximum items to show in cart badge before showing "9+" */
export const MAX_CART_BADGE_COUNT = 9;
